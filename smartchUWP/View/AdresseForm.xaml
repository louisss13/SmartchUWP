<Page
    x:Class="smartchUWP.View.AdresseForm"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:smartchUWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    
    >

    <StackPanel >
        <TextBlock Text="Adresse"/>
        <TextBox Header="Rue" Name="Street" Text="{Binding Street, Mode=TwoWay}"/>
        <TextBox Header="Numéro" Name="Number" Text="{Binding Number, Mode=TwoWay}"/>
        <TextBox Header="Boite" Name="Box" Text="{Binding Box, Mode=TwoWay}" />
        <TextBox Header="Ville" Name="City" Text="{Binding City , Mode=TwoWay}"/>
        <TextBox Header="Code Postal" Name="Zipcode" Text="{Binding Zipcode, Mode=TwoWay}"/>

    </StackPanel>
</Page>
