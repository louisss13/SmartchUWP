<Page
    x:Class="smartchUWP.View.Login"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:smartchUWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    DataContext="{Binding Source={StaticResource Locator} , Path=Login}"
    >

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="Login" Style="{StaticResource TitleTextBlockStyle}"/>
            <TextBlock Name="TextErreur" Text="Erreur lors de la connexion" Visibility="Collapsed" />
            <TextBox x:Name="Email" Header="Email" Margin="0,24,0,0" MaxLength="50" Width="400" HorizontalAlignment="Left" InputScope="EmailNameOrAddress" Text="{Binding Email}" />
            <PasswordBox x:Name="Password" Header="Password" Margin="0,24,0,0" MaxLength="50" Width="400" HorizontalAlignment="Left" InputScope="Password" Password="{Binding Password}" />
            <Button Content="Log in " Margin="0,24" Command="{Binding CommandLogin}"/>
            <Button Content="Register" Margin="0,24" Command="{Binding CommandNavigateRegister}"/>
           
        </StackPanel>
    </Grid>
</Page>
